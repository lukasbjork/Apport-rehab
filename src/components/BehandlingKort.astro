---
import type { Treatment } from '../lib/content';

interface Props {
  treatment: Treatment;
}
const { treatment } = Astro.props;
---

<a
  href={`/behandlingar/${treatment.slug}/`}
  class="stig group relative flex flex-col overflow-hidden rounded-2xl bg-papper transition-all duration-300 hover:-translate-y-1 hover:shadow-mjuk"
>
  <span
    aria-hidden="true"
    class="absolute inset-x-0 top-0 z-10 h-[2px] origin-left scale-x-0 bg-mossa transition-transform duration-400 ease-out group-hover:scale-x-100"
  ></span>

  {
    treatment.image && (
      <div class="aspect-16/10 overflow-hidden rounded-t-2xl bg-lind">
        <img
          src={treatment.image.src}
          alt={treatment.image.alt}
          width="1400"
          height="875"
          loading="lazy"
          decoding="async"
          class="h-full w-full object-cover transition-transform duration-500 group-hover:scale-[1.03]"
        />
      </div>
    )
  }

  <div class="flex flex-col p-7">
    <div>
      <div class="flex flex-wrap items-center gap-2">
        {
          treatment.regenerative && (
            <span class="rounded-full bg-lind px-2.5 py-1 text-[0.68rem] font-semibold uppercase tracking-[0.1em] text-mossa">
              Regenerativ
            </span>
          )
        }
        {
          !treatment.inAgreement && (
            <span class="rounded-full bg-tegel/10 px-2.5 py-1 text-[0.68rem] font-semibold uppercase tracking-[0.1em] text-tegel">
              Utanför avtalet
            </span>
          )
        }
      </div>

      <h3 class="mt-3.5 text-[1.28rem] leading-snug text-mossa-djup">{treatment.name}</h3>
      <p class="mt-3 text-[0.95rem] leading-relaxed text-skiffer">{treatment.teaser}</p>
    </div>

    <span
      class="mt-6 inline-flex items-center gap-2 text-[0.88rem] font-medium text-mossa transition-colors group-hover:text-tegel"
    >
      Läs mer
      <svg
        width="15"
        height="10"
        viewBox="0 0 15 10"
        fill="none"
        aria-hidden="true"
        class="transition-transform duration-300 group-hover:translate-x-1"
      >
        <path
          d="M0 5h13M9.5 1L13.5 5L9.5 9"
          stroke="currentColor"
          stroke-width="1.3"
          stroke-linecap="round"
          stroke-linejoin="round"></path>
      </svg>
    </span>
  </div>
</a>
